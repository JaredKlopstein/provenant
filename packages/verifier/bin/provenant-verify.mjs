#!/usr/bin/env node
// Phase 2. Deliberately fails loudly rather than pretending to verify anything.
console.error(
  'provenant-verify is not implemented yet (Phase 2).\n' +
    'For local chain integrity today, run: provenant chain verify --json',
);
process.exit(2);
